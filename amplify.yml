version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands: []        # no build step
  artifacts:
    baseDirectory: /      # <— publish from repo root (where index.html is)
    files:
      - '**/*'
  cache:
    paths: []
